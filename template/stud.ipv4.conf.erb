#/bin/bash
# The DAEMONARGS variable must remain consistent across all copies of this conf.
# Each configuration file carries a seperate chain of options to be used by stud.
# Generated by Chef for <%= node[:fqdn] %>

DAEMONARGS="-q --ssl -b <%= node[:network][:float][:v4_local] %>, <%= node[:network][:float][:back_port] %> -f <%= node[:network][:float][:v4] %>,<%= node[:network][:float][:front_port] %> --write-proxy /etc/ssl/private/doot.pem"
